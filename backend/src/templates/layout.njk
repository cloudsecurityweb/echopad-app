<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Echopad Notification</title>
  <style>
    body {
      font-family: 'Inter', system-ui, -apple-system, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif, 'Apple Color Emoji', 'Segoe UI Emoji';
      line-height: 1.6;
      color: #475569; /* Slate-600: Softer than black */
      margin: 0;
      padding: 0;
      background-color: #f1f5f9; /* Slate-100: Lighter background */
      -webkit-font-smoothing: antialiased;
    }
    .wrapper {
      width: 100%;
      background-color: #f1f5f9;
      padding: 40px 0;
    }
    .container {
      max-width: 560px; /* Slightly narrower for better readability */
      margin: 0 auto;
      background-color: #ffffff;
      border-radius: 16px; /* More rounded */
      overflow: hidden;
      /* Elegant, layered shadow */
      box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.05), 0 4px 6px -2px rgba(0, 0, 0, 0.025);
      border: 1px solid #e2e8f0; /* Subtle border for definition */
    }
    .header {
      background: #ffffff;
      padding: 32px 40px;
      /* Removed border-bottom for a cleaner, unified card look */
      text-align: center; /* Center logo for impact */
    }
    .logo-container {
      display: inline-flex;
      align-items: center;
      text-decoration: none;
      gap: 12px;
      padding: 8px 16px;
      background: #f8fafc;
      border-radius: 9999px; /* Pill shape */
      border: 1px solid #e2e8f0;
    }
    .logo-img {
      height: 28px;
      width: 28px;
      display: block;
      object-fit: contain;
    }
    .logo-text {
      font-size: 18px;
      font-weight: 700;
      color: #1e293b; /* Slate-800 */
      text-decoration: none;
      line-height: 1;
      letter-spacing: -0.025em;
    }
    .logo-accent {
      color: #06b6d4; /* Cyan-500 */
      font-weight: 800;
    }
    /* MSO Client Support (Outlook) */
    /*[if mso]>
    .logo-text {
      font-family: Helvetica, Arial, sans-serif !important;
    }
    <![endif]*/
    .content {
      padding: 0 40px 40px 40px; /* Aligned with header padding */
    }
    .footer {
      background-color: #f8fafc;
      padding: 32px;
      text-align: center;
      font-size: 12px;
      color: #94a3b8; /* Slate-400 */
      border-top: 1px solid #f1f5f9;
    }
    .button {
      display: inline-block;
      padding: 14px 32px; /* Larger click area */
      background: linear-gradient(135deg, #06b6d4, #2563eb); /* Richer gradient angle */
      color: white !important;
      text-decoration: none;
      border-radius: 12px; /* Smooth corners */
      font-weight: 600;
      font-size: 16px;
      text-align: center;
      margin: 32px 0;
      box-shadow: 0 4px 6px -1px rgba(6, 182, 212, 0.25); /* Colored shadow glow */
      width: auto;
      min-width: 160px;
    }
    h1, h2, h3 {
      color: #1e293b; /* Slate-800 */
      margin-top: 0;
      letter-spacing: -0.025em;
    }
    h2 {
      font-size: 24px;
      margin-bottom: 24px;
      text-align: center; /* Center main heading */
    }
    p {
      margin-bottom: 16px;
      font-size: 16px;
      color: #334155; /* Slate-700 */
    }
    .highlight-box {
      background-color: #f8fafc; /* Slate-50 */
      border: 1px solid #cbd5e1; /* Slate-300 */
      padding: 20px;
      border-radius: 12px;
      margin: 24px 0;
      text-align: center; /* Center content in box */
    }
    .label {
      font-size: 11px;
      text-transform: uppercase;
      letter-spacing: 0.1em;
      color: #64748b; /* Slate-500 */
      font-weight: 700;
      margin-bottom: 8px;
      display: block;
    }
    .value {
      font-family: 'SFMono-Regular', Consolas, 'Liberation Mono', Menlo, monospace;
      font-size: 20px; /* Larger code */
      color: #0f172a; /* Slate-900 */
      font-weight: 700;
      background: #e2e8f0; /* Slate-200 highlight */
      padding: 8px 16px;
      border-radius: 8px;
      display: inline-block;
      letter-spacing: 0.05em;
    }
    .expiry-note {
      font-size: 13px;
      color: #94a3b8;
      font-style: italic;
      margin-top: 24px;
      text-align: center;
    }
    a {
      color: #06b6d4;
      text-decoration: none;
      font-weight: 500;
    }
    @media only screen and (max-width: 600px) {
      .container { width: 100% !important; border-radius: 0; border: none; }
      .wrapper { padding: 0; }
      .content { padding: 24px; }
      .header { padding: 24px; }
      .button { display: block; width: auto; }
    }
  </style>
</head>
<body>
  <div class="wrapper">
    <div class="container">
      <div class="header">
        <a href="{{ frontendUrl }}" class="logo-container">
          <!-- <img src="{{ logoUrl | safe }}" alt="Echopad Logo" class="logo-img" width="32" height="32" style="display:block; border:0;"> -->
          <span class="logo-text">Echopad <span class="logo-accent">AI</span></span>
        </a>
      </div>
      <div class="content">
        {% block content %}{% endblock %}
      </div>
      <div class="footer">
        <p>© {{ year }} Echopad. All rights reserved.</p>
        <p>This is an automated message, please do not reply.</p>
      </div>
    </div>
  </div>
</body>
</html>
