/**
 * Aperio app URL for redirects and links.
 * Set VITE_APERIO_URL when frontend and backend are on different origins (e.g. dev: http://localhost:3000/aperio).
 */
export const APERIO_APP_URL = import.meta.env.VITE_APERIO_URL || "/aperio";
